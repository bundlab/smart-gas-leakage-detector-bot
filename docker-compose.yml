version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: gasdb
      POSTGRES_USER: gasuser
      POSTGRES_PASSWORD: gaspass